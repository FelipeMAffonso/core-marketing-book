<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<title>MKTG 4333 · Chapter 4 · How Buyers Buy deck</title>
<style>
  :root{
    --bg:#f5f2ec; --ink:#1a1a1a; --ink-soft:#4A4A4A; --ink-mute:#7A7A7A;
    --rule:#E8E6E1; --accent:#DA7756; --accent-tint:#FDF6F3;
    --font:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }
  *{ margin:0; padding:0; box-sizing:border-box; }
  html,body{ background:var(--bg); font-family:var(--font); color:var(--ink); }
  body{ padding:64px 72px 96px; }
  header{
    display:flex; justify-content:space-between; align-items:flex-end;
    border-bottom:1px solid var(--rule); padding-bottom:20px; margin-bottom:36px;
  }
  header .dot{ display:inline-block; width:10px; height:10px; background:var(--accent); border-radius:50%; margin-right:10px; vertical-align:middle; }
  header h1{ font-size:32px; font-weight:600; letter-spacing:-0.01em; }
  header h1 em{ font-style:italic; color:var(--accent); font-weight:500; }
  header .meta{
    font-size:11px; letter-spacing:0.12em; text-transform:uppercase;
    color:var(--ink-mute); font-weight:600;
  }
  .intro{
    max-width:70ch; font-size:18px; line-height:1.55; color:var(--ink-soft);
    margin-bottom:44px;
  }
  .intro a{ color:var(--accent); text-decoration:none; border-bottom:1px solid var(--accent); }
  .grid{
    display:grid; grid-template-columns: repeat(auto-fill, minmax(420px, 1fr));
    gap:28px;
  }
  .card{
    background:#fff; border:1px solid var(--rule);
    display:flex; flex-direction:column; overflow:hidden;
    transition: transform .2s, box-shadow .2s;
  }
  .card:hover{ transform:translateY(-2px); box-shadow: 0 8px 24px -10px rgba(0,0,0,.15); }
  .card .frame{
    position:relative; aspect-ratio: 16 / 9; overflow:hidden; background:#ffffff;
    border-bottom:1px solid var(--rule);
  }
  .card .frame .scaler{
    position:absolute; top:0; left:0;
    width:960px; height:540px;
    transform-origin: top left;
  }
  .card .frame iframe{
    width:960px; height:540px; border:0; display:block; pointer-events:none;
    background:#ffffff;
  }
  .card .info{ padding:16px 22px; display:flex; justify-content:space-between; align-items:baseline; gap:12px; }
  .card .info h3{ font-size:16px; font-weight:600; line-height:1.3; }
  .card .info .n{
    font-size:11px; font-weight:600; letter-spacing:0.14em; color:var(--ink-mute);
    text-transform:uppercase; white-space:nowrap;
  }
  .card .info .tmpl{ display:block; font-size:12px; color:var(--ink-soft); margin-top:3px; font-style:italic; }
  .card a.open{
    display:block; text-align:right; font-size:11px; letter-spacing:0.12em;
    text-transform:uppercase; color:var(--accent); font-weight:600;
    padding:0 22px 16px; text-decoration:none;
  }
  footer{
    margin-top:64px; padding-top:20px; border-top:1px solid var(--rule);
    font-size:13px; color:var(--ink-mute); display:flex; justify-content:space-between;
  }
</style>
</head>
<body>
  <header>
    <h1><span class="dot"></span>Chapter 4 · <em>How buyers buy</em>.</h1>
    <span class="meta">MKTG 4333 · 20 slides · 960 × 540</span>
  </header>

  <p class="intro">
    A teaching deck on consumer behavior, built on the coral design system. One claim per slide, white
    backgrounds, one coral element per slide, Segoe UI throughout. It pairs with the Chapter 4 chapter and runs
    the same arc: memory as the first storefront and mental availability as a web, the autopilot most buying
    runs on, the rarer state when buyers actually think (the MAO triad and the two ELM routes), the four
    families of shortcuts that absorb Cialdini's seven principles, consideration sets built before search, and
    the AI shelf now writing the shortlist. Read the system rules in
    <a href="../DESIGN-SYSTEM.md">DESIGN-SYSTEM.md</a> and
    <a href="../CHART-PRINCIPLES.md">CHART-PRINCIPLES.md</a>.
  </p>

  <div class="grid" id="grid"></div>

<script>
(() => {
  const slides = [
    ["01-title.html",              "01", "How buyers buy, and why the brand retrieved first wins", "Title / cover"],
    ["02-divider-memory.html",     "02", "Buyers retrieve, then choose", "Section divider"],
    ["03-associative-network.html","03", "Mental availability is a web, not a list", "Exhibit · associative network"],
    ["04-awareness-not-retrieval.html","04", "Awareness is not retrieval", "Comparison bars"],
    ["05-shape-vs-color.html",     "05", "Shape-based assets survive the glance", "Comparison bars"],
    ["06-divider-autopilot.html",  "06", "Most buying runs on autopilot", "Section divider"],
    ["07-habit-share.html",        "07", "About 43% of daily action repeats in context", "KPI row"],
    ["08-glp1-cue-disruption.html","08", "A cue-disruption shock hits cue-built categories hardest", "Comparison bars"],
    ["09-lewin-stanley.html",      "09", "The environment moved, not the buyer", "Two-column"],
    ["10-divider-thinking.html",   "10", "When buyers actually think", "Section divider"],
    ["11-mao-triad.html",          "11", "All three gates of deliberation must open at once", "Exhibit · MAO triad"],
    ["12-elm-routes.html",         "12", "High MAO routes central, low routes peripheral", "Exhibit · ELM dual route"],
    ["13-divider-shortcuts.html",  "13", "Shortcuts shape the choice", "Section divider"],
    ["14-four-families.html",      "14", "Four families absorb Cialdini's seven principles", "Exhibit · four families"],
    ["15-loss-aversion.html",      "15", "A loss weighs about twice a gain", "Exhibit · value function"],
    ["16-divider-consideration.html","16", "Consideration sets are built before search", "Section divider"],
    ["17-consideration-set.html",  "17", "The first recalled wins roughly 30% before any comparison", "Exhibit · consideration set"],
    ["18-ai-shelf.html",           "18", "The model now writes the consideration set", "Exhibit · the AI shelf"],
    ["19-algorithm-aversion.html", "19", "Buyers resist the AI pick, except when inventory is vanishing", "Two-column"],
    ["20-closing.html",            "20", "Run the new-shelf audit on a brand you actually buy", "Closing / takeaways"]
  ];
  const grid = document.getElementById('grid');
  slides.forEach(([href, n, claim, tmpl]) => {
    const card = document.createElement('article');
    card.className = 'card';
    card.innerHTML =
      '<div class="frame"><div class="scaler"><iframe src="' + href + '?thumb=1" title="' + claim + '"></iframe></div></div>' +
      '<div class="info"><div><h3>' + claim + '</h3><span class="tmpl">' + tmpl + '</span></div><span class="n">' + n + '</span></div>' +
      '<a class="open" href="' + href + '" target="_blank">Open &rarr;</a>';
    grid.appendChild(card);
  });

  function fitCards(){
    document.querySelectorAll('.card .frame').forEach(frame => {
      const scaler = frame.querySelector('.scaler');
      if (!scaler) return;
      const w = frame.clientWidth;
      const s = w / 960;
      scaler.style.transform = 'scale(' + s + ')';
      scaler.style.width  = '960px';
      scaler.style.height = '540px';
      frame.style.height = (540 * s) + 'px';
    });
  }
  document.querySelectorAll('iframe').forEach(f => {
    f.addEventListener('load', () => {
      try { f.contentDocument.documentElement.classList.add('is-thumb'); } catch(_){}
    });
  });
  window.addEventListener('resize', fitCards);
  window.addEventListener('load', fitCards);
  fitCards();
})();
</script>

  <footer>
    <span>MKTG 4333 · Prof. Felipe M. Affonso · Spears School of Business</span>
    <span>White bg · Segoe UI · one coral accent · pairs with the Ch 4 chapter</span>
  </footer>
</body>
</html>
