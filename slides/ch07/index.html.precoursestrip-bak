<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<title>MKTG 4333 · Chapter 7 · Brands deck</title>
<style>
  :root{
    --bg:#f5f2ec; --ink:#1a1a1a; --ink-soft:#4A4A4A; --ink-mute:#7A7A7A;
    --rule:#E8E6E1; --accent:#DA7756; --accent-tint:#FDF6F3;
    --font:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }
  *{ margin:0; padding:0; box-sizing:border-box; }
  html,body{ background:var(--bg); font-family:var(--font); color:var(--ink); }
  body{ padding:64px 72px 96px; }
  header{
    display:flex; justify-content:space-between; align-items:flex-end;
    border-bottom:1px solid var(--rule); padding-bottom:20px; margin-bottom:36px;
  }
  header .dot{ display:inline-block; width:10px; height:10px; background:var(--accent); border-radius:50%; margin-right:10px; vertical-align:middle; }
  header h1{ font-size:32px; font-weight:600; letter-spacing:-0.01em; }
  header h1 em{ font-style:italic; color:var(--accent); font-weight:500; }
  header .meta{
    font-size:11px; letter-spacing:0.12em; text-transform:uppercase;
    color:var(--ink-mute); font-weight:600;
  }
  .intro{
    max-width:70ch; font-size:18px; line-height:1.55; color:var(--ink-soft);
    margin-bottom:44px;
  }
  .intro a{ color:var(--accent); text-decoration:none; border-bottom:1px solid var(--accent); }
  .grid{
    display:grid; grid-template-columns: repeat(auto-fill, minmax(420px, 1fr));
    gap:28px;
  }
  .card{
    background:#fff; border:1px solid var(--rule);
    display:flex; flex-direction:column; overflow:hidden;
    transition: transform .2s, box-shadow .2s;
  }
  .card:hover{ transform:translateY(-2px); box-shadow: 0 8px 24px -10px rgba(0,0,0,.15); }
  .card .frame{
    position:relative; aspect-ratio: 16 / 9; overflow:hidden; background:#ffffff;
    border-bottom:1px solid var(--rule);
  }
  .card .frame .scaler{
    position:absolute; top:0; left:0;
    width:960px; height:540px;
    transform-origin: top left;
  }
  .card .frame iframe{
    width:960px; height:540px; border:0; display:block; pointer-events:none;
    background:#ffffff;
  }
  .card .info{ padding:16px 22px; display:flex; justify-content:space-between; align-items:baseline; gap:12px; }
  .card .info h3{ font-size:16px; font-weight:600; line-height:1.3; }
  .card .info .n{
    font-size:11px; font-weight:600; letter-spacing:0.14em; color:var(--ink-mute);
    text-transform:uppercase; white-space:nowrap;
  }
  .card .info .tmpl{ display:block; font-size:12px; color:var(--ink-soft); margin-top:3px; font-style:italic; }
  .card a.open{
    display:block; text-align:right; font-size:11px; letter-spacing:0.12em;
    text-transform:uppercase; color:var(--accent); font-weight:600;
    padding:0 22px 16px; text-decoration:none;
  }
  footer{
    margin-top:64px; padding-top:20px; border-top:1px solid var(--rule);
    font-size:13px; color:var(--ink-mute); display:flex; justify-content:space-between;
  }
</style>
</head>
<body>
  <header>
    <h1><span class="dot"></span>Chapter 7 · <em>Brands and brand management</em>.</h1>
    <span class="meta">MKTG 4333 · 20 slides · 960 × 540</span>
  </header>

  <p class="intro">
    A teaching deck on the brand as a network of memory, built on the coral design system. One claim per slide,
    white backgrounds, one coral element per slide, Segoe UI throughout. It pairs with the Chapter 7 lecture
    video and runs the same arc: the Tylenol puzzle, the brand-as-network, mental availability and category
    entry points, distinctive assets, the growth laws, brand equity, positioning, and the diagnostic. Read the
    system rules in
    <a href="../DESIGN-SYSTEM.md">DESIGN-SYSTEM.md</a> and
    <a href="../CHART-PRINCIPLES.md">CHART-PRINCIPLES.md</a>.
  </p>

  <div class="grid" id="grid"></div>

<script>
(() => {
  const slides = [
    ["01-title.html",                  "01", "Brands and brand management", "Title / cover"],
    ["02-divider-network.html",        "02", "The whole chapter is one network", "Section divider"],
    ["03-tylenol-puzzle.html",         "03", "Tylenol fell to 7% and was back near 30% in a year", "Trend · the puzzle"],
    ["04-brand-is-network.html",       "04", "Remove the can and the words still pull the network", "Content · the network"],
    ["05-mental-availability-cep.html","05", "A rival linked to lunch alone competes for one ninth of the footprint", "Exhibit · chapter CEP wheel"],
    ["06-ninety-five-five.html",       "06", "Brand work plants memory in the 95% who buy nothing this week", "KPI row"],
    ["07-distinctive-asset.html",      "07", "A cue brings one brand to mind before the name is read", "Exhibit · chapter asset gallery"],
    ["08-shape-audit.html",            "08", "The Coke bottle scores 89; Stanley sits at 43, below the line", "Comparison bars"],
    ["09-fame-uniqueness.html",        "09", "Each cue sorts into one of four verdicts", "Exhibit · chapter Fame×Uniqueness"],
    ["10-distinctive-not-different.html","10","The other nine in ten bought on the cue that came to mind", "Two-column"],
    ["11-divider-growth.html",         "11", "Watch real buyers and the same patterns repeat", "Section divider"],
    ["12-penetration.html",            "12", "New-buyer campaigns beat existing-buyer campaigns about three to one", "Comparison bars"],
    ["13-double-jeopardy.html",        "13", "The gap from seven to nine is far too small to rescue the small brand", "Exhibit · chapter DJ line"],
    ["14-mental-x-physical.html",      "14", "A zero on either axis is a zero overall", "Exhibit · chapter mental×physical"],
    ["15-divider-equity.html",         "15", "Now put a dollar number on it", "Section divider"],
    ["16-brand-equity-levers.html",    "16", "The brand is the value of CLV attributable to the brand", "Content · the three levers"],
    ["17-tylenol-perrier.html",        "17", "Same response speed, opposite outcomes", "Two-column"],
    ["18-positioning-onliness.html",   "18", "A position survives only when a rival's name breaks the sentence", "Content · positioning"],
    ["19-bud-light.html",              "19", "The position was not the failure; the missing record was", "Pull quote"],
    ["20-closing-diagnostic.html",     "20", "Run the four-step audit on a brand you actually use", "Closing / takeaways"]
  ];
  const grid = document.getElementById('grid');
  slides.forEach(([href, n, claim, tmpl]) => {
    const card = document.createElement('article');
    card.className = 'card';
    card.innerHTML =
      '<div class="frame"><div class="scaler"><iframe src="' + href + '?thumb=1" title="' + claim + '"></iframe></div></div>' +
      '<div class="info"><div><h3>' + claim + '</h3><span class="tmpl">' + tmpl + '</span></div><span class="n">' + n + '</span></div>' +
      '<a class="open" href="' + href + '" target="_blank">Open &rarr;</a>';
    grid.appendChild(card);
  });

  function fitCards(){
    document.querySelectorAll('.card .frame').forEach(frame => {
      const scaler = frame.querySelector('.scaler');
      if (!scaler) return;
      const w = frame.clientWidth;
      const s = w / 960;
      scaler.style.transform = 'scale(' + s + ')';
      scaler.style.width  = '960px';
      scaler.style.height = '540px';
      frame.style.height = (540 * s) + 'px';
    });
  }
  document.querySelectorAll('iframe').forEach(f => {
    f.addEventListener('load', () => {
      try { f.contentDocument.documentElement.classList.add('is-thumb'); } catch(_){}
    });
  });
  window.addEventListener('resize', fitCards);
  window.addEventListener('load', fitCards);
  fitCards();
})();
</script>

  <footer>
    <span>MKTG 4333 · Prof. Felipe M. Affonso · Spears School of Business</span>
    <span>White bg · Segoe UI · one coral accent · pairs with the Ch 7 video</span>
  </footer>
</body>
</html>
