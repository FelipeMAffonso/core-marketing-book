<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<title>MKTG 4333 · Chapter 6 · STP deck</title>
<style>
  :root{
    --bg:#f5f2ec; --ink:#1a1a1a; --ink-soft:#4A4A4A; --ink-mute:#7A7A7A;
    --rule:#E8E6E1; --accent:#DA7756; --accent-tint:#FDF6F3;
    --font:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }
  *{ margin:0; padding:0; box-sizing:border-box; }
  html,body{ background:var(--bg); font-family:var(--font); color:var(--ink); }
  body{ padding:64px 72px 96px; }
  header{
    display:flex; justify-content:space-between; align-items:flex-end;
    border-bottom:1px solid var(--rule); padding-bottom:20px; margin-bottom:36px;
  }
  header .dot{ display:inline-block; width:10px; height:10px; background:var(--accent); border-radius:50%; margin-right:10px; vertical-align:middle; }
  header h1{ font-size:32px; font-weight:600; letter-spacing:-0.01em; }
  header h1 em{ font-style:italic; color:var(--accent); font-weight:500; }
  header .meta{
    font-size:11px; letter-spacing:0.12em; text-transform:uppercase;
    color:var(--ink-mute); font-weight:600;
  }
  .intro{
    max-width:70ch; font-size:18px; line-height:1.55; color:var(--ink-soft);
    margin-bottom:44px;
  }
  .intro a{ color:var(--accent); text-decoration:none; border-bottom:1px solid var(--accent); }
  .grid{
    display:grid; grid-template-columns: repeat(auto-fill, minmax(420px, 1fr));
    gap:28px;
  }
  .card{
    background:#fff; border:1px solid var(--rule);
    display:flex; flex-direction:column; overflow:hidden;
    transition: transform .2s, box-shadow .2s;
  }
  .card:hover{ transform:translateY(-2px); box-shadow: 0 8px 24px -10px rgba(0,0,0,.15); }
  .card .frame{
    position:relative; aspect-ratio: 16 / 9; overflow:hidden; background:#ffffff;
    border-bottom:1px solid var(--rule);
  }
  .card .frame .scaler{
    position:absolute; top:0; left:0;
    width:960px; height:540px;
    transform-origin: top left;
  }
  .card .frame iframe{
    width:960px; height:540px; border:0; display:block; pointer-events:none;
    background:#ffffff;
  }
  .card .info{ padding:16px 22px; display:flex; justify-content:space-between; align-items:baseline; gap:12px; }
  .card .info h3{ font-size:16px; font-weight:600; line-height:1.3; }
  .card .info .n{
    font-size:11px; font-weight:600; letter-spacing:0.14em; color:var(--ink-mute);
    text-transform:uppercase; white-space:nowrap;
  }
  .card .info .tmpl{ display:block; font-size:12px; color:var(--ink-soft); margin-top:3px; font-style:italic; }
  .card a.open{
    display:block; text-align:right; font-size:11px; letter-spacing:0.12em;
    text-transform:uppercase; color:var(--accent); font-weight:600;
    padding:0 22px 16px; text-decoration:none;
  }
  footer{
    margin-top:64px; padding-top:20px; border-top:1px solid var(--rule);
    font-size:13px; color:var(--ink-mute); display:flex; justify-content:space-between;
  }
</style>
</head>
<body>
  <header>
    <h1><span class="dot"></span>Chapter 6 · <em>Segmentation, targeting, positioning</em>.</h1>
    <span class="meta">MKTG 4333 · 20 slides · 960 × 540</span>
  </header>

  <p class="intro">
    A teaching deck on the STP funnel, built on the coral design system. One claim per slide, white
    backgrounds, one coral element per slide, Segoe UI throughout. It pairs with the Chapter 6 lecture
    video. Read the system rules in
    <a href="../DESIGN-SYSTEM.md">DESIGN-SYSTEM.md</a> and
    <a href="../CHART-PRINCIPLES.md">CHART-PRINCIPLES.md</a>.
  </p>

  <div class="grid" id="grid"></div>

<script>
(() => {
  const slides = [
    ["01-title.html",            "01", "Segmentation, targeting, and positioning", "Title / cover"],
    ["02-divider-funnel.html",   "02", "The whole chapter is one funnel", "Section divider"],
    ["03-stp-funnel.html",       "03", "STP runs in one direction", "Exhibit · chapter STP funnel"],
    ["04-why-heterogeneity.html","04", "Ignore the differences and you serve a customer who exists nowhere", "Content"],
    ["05-four-bases.html",       "05", "The benefit a buyer wants is the cause of the choice", "Content · four bases"],
    ["06-knorr-demand-space.html","06","Knorr sold the same soup 127% better by changing the occasion", "Comparison bars"],
    ["07-five-criteria.html",    "07", "Fail one of five tests and it is research", "Content · the filter"],
    ["08-divider-targeting.html","08", "Targeting hunts for the segment you can win", "Section divider"],
    ["09-win-not-size.html",     "09", "Score compatibility before attractiveness", "Two-column"],
    ["10-customer-profile.html", "10", "Translate the strategy into descriptors you can buy", "Content · the bridge"],
    ["11-targeting-ethics.html", "11", "The model was right; acting on it was the violation", "Pull quote"],
    ["12-divider-positioning.html","12","Positioning is work you do to the buyer's mind", "Section divider"],
    ["13-mental-ladder.html",    "13", "The top rung is the default answer", "Content · mental ladder"],
    ["14-pop-pod.html",          "14", "Parity earns a place; difference wins the choice", "Exhibit · chapter POP/POD"],
    ["15-perceptual-map.html",   "15", "An empty quadrant is an opening only when buyers want it", "Exhibit · chapter map"],
    ["16-liquid-death.html",     "16", "The label alone earned 27% more revenue per unit", "Comparison bars"],
    ["17-double-jeopardy.html",  "17", "The loyalty gap is far too small to rescue the small brand", "Comparison bars"],
    ["18-availability.html",     "18", "Both easy to think of and easy to buy", "Two-column"],
    ["19-client-diagnostic.html","19", "Run a four-step diagnostic on a brand you buy", "Content · diagnostic"],
    ["20-closing.html",          "20", "STP picks the rung the brand asks to be remembered on", "Closing / takeaways"]
  ];
  const grid = document.getElementById('grid');
  slides.forEach(([href, n, claim, tmpl]) => {
    const card = document.createElement('article');
    card.className = 'card';
    card.innerHTML =
      '<div class="frame"><div class="scaler"><iframe src="' + href + '?thumb=1" title="' + claim + '"></iframe></div></div>' +
      '<div class="info"><div><h3>' + claim + '</h3><span class="tmpl">' + tmpl + '</span></div><span class="n">' + n + '</span></div>' +
      '<a class="open" href="' + href + '" target="_blank">Open &rarr;</a>';
    grid.appendChild(card);
  });

  function fitCards(){
    document.querySelectorAll('.card .frame').forEach(frame => {
      const scaler = frame.querySelector('.scaler');
      if (!scaler) return;
      const w = frame.clientWidth;
      const s = w / 960;
      scaler.style.transform = 'scale(' + s + ')';
      scaler.style.width  = '960px';
      scaler.style.height = '540px';
      frame.style.height = (540 * s) + 'px';
    });
  }
  document.querySelectorAll('iframe').forEach(f => {
    f.addEventListener('load', () => {
      try { f.contentDocument.documentElement.classList.add('is-thumb'); } catch(_){}
    });
  });
  window.addEventListener('resize', fitCards);
  window.addEventListener('load', fitCards);
  fitCards();
})();
</script>

  <footer>
    <span>MKTG 4333 · Prof. Felipe M. Affonso · Spears School of Business</span>
    <span>White bg · Segoe UI · one coral accent · pairs with the Ch 6 video</span>
  </footer>
</body>
</html>
